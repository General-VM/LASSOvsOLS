---
title: "LASSO vs OLS"
author: "Victor Matyiku, Rebecca Ly"
date: "2025-03-06"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Code

## Generating data

```{r}
p <- 10
n <- 100

beta_0 <- rnorm(1)
coefs <- matrix(rnorm(p), nrow = p, ncol = 1)

predictors <- matrix(rnorm(n*p), nrow = n, ncol = p)

errors <- rnorm(100)
observations <- beta_0 + predictors %*% coefs + errors

generated_data <- as.data.frame(
  cbind(observations, predictors)
) #V1 is Y and the rest are the predictors
head(generated_data)
```

## OLS

```{r}
ols_model <- lm(V1 ~ ., generated_data)
significant_coefs <- data.frame(Value = summary(ols_model)$coef[summary(ols_model)$coef[,4] <= .05, 4])
significant_coefs
```

```{r}
if (is.na(significant_coefs["(Intercept)",])) {
  ols_beta <- 0
  ols_coefs <- significant_coefs
} else {
  ols_beta <- significant_coefs["(Intercept)",]
}

ols_coefs <- significant_coefs[row.names(significant_coefs) != "(Intercept)", , drop = FALSE]
ols_coefs
```

````{r}

```